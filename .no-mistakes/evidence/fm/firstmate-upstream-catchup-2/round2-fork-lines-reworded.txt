.agents/skills/afk/SKILL.md: The operational prefix travels with the message text; it does not rely on harness-level typed-vs-injected detection, which is not portable across claude, opencode, grok, and kimi.
.agents/skills/afk/SKILL.md: If that submit cannot be confirmed, it raises a wedge alarm:
.agents/skills/afk/SKILL.md: A repeated `check` wake with the same key replaces its earlier buffered item, tagged `{check:<key>}`, instead of adding another, so one recurring check yields one digest item.
bin/fm-supervise-daemon.sh:   local state=$1 item=$2 key=${3:-} buf prefix replacement
docs/captain-hold-lifecycle.md: The optional mode column carries the selected choice's close: `done` (default) completes the task, `release` lifts the hold so held work resumes, and `defer:YYYY-MM-DD` re-holds an open captain call until that future date under its existing hold reason, appends a `Captain deferred this call until <date> through <source>: later (answer as shown to the captain: <label>)` provenance line to the body, omitting the parenthetical when the choice carries no label, and is reported as `deferred:` with no resolution record; any other value is skipped.
docs/captain-hold-lifecycle.md: A dated deferral is only for the exact answer `later`, and `later` is only accepted with one, so neither can close a call by accident.
docs/captain-hold-lifecycle.md: `bin/fm-procevent-lavish.sh answers` is one such adapter command; it reads only rows tagged `choice`, relays the selected option's close mode, and can never let freeform captain prose forge a task id or a mode.
docs/captain-hold-lifecycle.md: The same branch drops its bare and separator-annotated later values, which carry no dated deferral; each dropped legacy row still replaces any earlier answer to the same card, so the call stays held instead of closing.
docs/captain-hold-lifecycle.md: It proves: cleanup of a finished task whose own row is the captain call leaves that call open, queued, held, carrying its deliverable, and visible in Bearings' Captain's Call, leaves no pending record behind, survives a `--force` cleanup, and closes only when `answer` records the captain's words, while an ordinary finished task in the same home still closes with its report link; an interrupted cleanup leaves the row In flight and untouched with its pending record, the next session start retains it as queued and held with the deliverable recorded when it remains unanswered, and an answer before replay preserves that record's completed report while closing the call so the next session start retires the satisfied record without losing the delivery from Recently Landed; a pending-close record that cannot be validated refuses the answer while naming the record and the reason; a relocated data directory keeps the retention in its one configured backlog; direct PR and local-only merge entrypoint calls refuse a still-held task before reaching the forge or moving local main, while a released pull request passes the guarded PR entrypoint, cleanup records its artifact, and Recently Landed publishes it; an ordinary release still survives zero-retention cleanup and archives when configured; a ship row whose captain hold cannot be read refuses cleanup before any destructive step and surfaces the read failure; the reconstructed silent-divergence case is signalled - a status resolution over a still-open captain-held task reaches both `diverged` and the drain's `RECORD DIVERGENCE` section, under the collapsed and the legacy identity alike, while the backlog task, its hold, and the status log all survive the report unchanged and the printed hint names both reconciliation directions; the false-signal boundary holds - a captain call with no routed work item, a verified `captain-held` transfer, a still-open status decision, an already answered call, and an ordinary task whose keyed question was answered all stay silent; a released call whose decision text is `local main`, closed with no artifact, is not published as a local-only landing; a report-only unresolved captain call refuses `--none` completion before teardown can erase the source; non-forced scout teardown always requires the durable inventory verification; the recorded-answer guard (a bare `tasks-axi done` close fails `verify` until `answer` records the captain's word, and an ordinary finished task cannot be dressed up as an answered call); answer-time resolution through a bound channel with task-id keys, including the `release` mode, a board `later` choice dating the open hold under its unchanged reason while `done` and `release` keep their modes, mode-matched replay idempotence, and the refusal of drifted, mode-mismatched, absent, unheld, and already-closed keys and of undated, invalid, past, or non-`later` deferrals; the chat channel reaching the same intake; hold-set stamping that precedes visible hold state, preserves an active lifecycle's timestamp, and resets after release; interrupted answer closure retaining the stamp until close and restoring resolution-first ordering on retry; deferral through `--until` leaving `captain_actionable` false until due; and every legacy path (composed identities through the shim, pre-collapse `decision_keys=` metadata, routed-resolution replay, and a concrete-origin binding).
docs/captain-hold-lifecycle.md: The captured-source coverage proves Lavish deduplicates each card before separating versioned structured selections from notes, bare and annotated Reconcile choices never reach keyed answers, genuine current and legacy choices still close normally, legacy bare and separator-annotated reconcile and later values feed neither intake, a legacy later that replaces an earlier answer to the same card leaves that call held, mixed repeated selections preserve every other card's final value, the generic runner creates a request only through a verified bound source, chat reconcile text creates none, and the resulting board request authorizes evidence-backed closure.
docs/captain-hold-lifecycle.md: The board's half is pinned in `tests/fm-bearings-board.test.sh`: every published decision card carries exactly one reconcile option, authored options reserve that value across every card type, recommendations name authored options, a `later` option requires a valid `defer_until` date that no other option may carry, a decision card whose structured subject appears in the payload's landed rows is dropped while a genuinely open one is kept even when an unrelated landed id contains its key after a newline, a build requires a fresh authoritative listed-open result before binding or arming, a reopen retires the pre-reopen source generation and waits for a fresh live listener, and a rebuild of an already-armed board with no live listener starts one.
docs/configuration.md: Today a Claude, Cursor, OpenCode, omp, Grok, or Codex primary runs it, and only for the away posture: with the file present, that primary's arm owner runs the host in the watcher arm's place, the host handles wakes on the engine while the away-posture record `state/.afk-contract` exists, and `/afk` launches no away daemon on that home, while `/quiet` still does except on Codex, which refuses `/quiet`.
docs/configuration.md: The shell scripts do not match those rules; firstmate chooses the best matching rule with judgment, resolves its profile object or array under the operating contract in `AGENTS.md` section 4, and passes only concrete `--harness`, `--model`, and `--effort` flags to `fm-spawn.sh`, except that a selected Grok Bot target goes to `bin/fm-grok-bot-dispatch.sh` instead (see below).
docs/configuration.md: The single-object form stays fully backward-compatible, and every profile needs `harness` unless it is a Grok Bot target.
docs/configuration.md: A Grok Bot target profile carries `grok_bot`, the name of one standing Bot, and optionally `off`; it takes no `harness`, `model`, `effort`, `provider`, or `floor`, because Grok Bot is not a launchable harness and runs on its own cloud computer with no worktree, hooks, or access to local files.
docs/configuration.md: Route it only file-free work such as web research and public-repository reading.
docs/configuration.md: When selection lands on it, firstmate sends the brief through `bin/fm-grok-bot-dispatch.sh` rather than `fm-spawn.sh`; that script's header owns its arguments, the home-private bridge it drives, and its outcomes.
docs/configuration.md: The reply comes back marked unverified, and firstmate has it checked under the independent-verification rule by a candidate from a vendor other than xAI before relaying or acting on it.
docs/configuration.md: When the Bot's usage is spent, firstmate supplies a `blocked` fact for that candidate so ordered selection moves to the next one.
docs/configuration.md: Grok Bot targets run only in the home that holds the bridge, which is the primary.
docs/configuration.md: A secondmate inherits the rules but not the bridge, so there `bin/fm-grok-bot-dispatch.sh` refuses with exit 2 and names this; firstmate then supplies a `launch_failed` fact for that candidate, treating the Bot target as unavailable so the rule falls to its other candidates.
docs/configuration.md: A `"select": "quota-balanced"` rule cannot use a Grok Bot target, because a Bot has no quota evidence; bootstrap, `fm-dispatch-select.sh`, and typed resolution all refuse that configuration.
docs/configuration.md: Typed resolution reports a Grok Bot target as eligible but unranked and never emits it as a `profile:` line.
docs/configuration.md: The typed resolver supplies the fixed neutral Choice option `No listed rule applies to this task.` for work that matches no listed rule.
docs/configuration.md: Optional rule `select` accepts `ordered` or `quota-balanced`; absent means `ordered`.
docs/configuration.md: Optional profile `off` must be a boolean and defaults to false; `off: true` excludes that profile in either selection mode.
docs/configuration.md: Ordered selection walks the configured list and picks the first available candidate, preserving its harness, model, and effort.
docs/configuration.md: A candidate is unavailable only when disabled or when firstmate supplies concrete evidence of a launch failure, a known block, or quota-axi reporting its applicable provider `exhausted_now` with `established` confidence.
docs/configuration.md: Lower headroom, `projected_exhaustion`, or `early` confidence never skip an ordered candidate.
docs/configuration.md: Firstmate establishes which provider and account evidence applies; the selector neither discovers credentials nor infers provider mappings.
docs/configuration.md: Explicit `quota-balanced` selection retains the procedure owned by `quota-array-dispatch`.
docs/configuration.md: If no dispatch rule fits, firstmate resolves `default` through the same object-or-array path; the static crewmate harness is the fallback only when no configured rule or default applies.
docs/configuration.md: If every candidate in the selected rule or default is unavailable, stop and report every skip instead of silently trying another rule or the static harness.
docs/configuration.md: `bin/fm-dispatch-select.sh` is the stateless selector for an already-selected rule index or `default`; its header and `--help` own arguments, availability-fact format, output, and exit codes.
docs/configuration.md: It prints the chosen profile and each prior skip reason, or an explicit quota-balanced handoff without choosing by order.
docs/configuration.md: `tests/fm-dispatch-select.test.sh` verifies these paths through its public command interface.
docs/configuration.md: Malformed JSON, an empty or malformed rule/default array, an unknown selection mode, a non-boolean `off`, an unverified harness, or an effort value unsupported by that harness is reported as `CREW_DISPATCH: invalid config/crew-dispatch.json - ...`.
docs/configuration.md: When TypeSafe returns a non-200 response or a request times out or fails, the resolver retries once through Vercel AI Gateway if `AI_GATEWAY_API_KEY` is present in the environment or the home's `.env`; the environment wins.
docs/configuration.md: Both requests send the same Jev Choice state and question, with the gateway's `typesafe-ai/jev` model identifier in place of the direct call's `jev-latest`; the result's `provider:` line names `typesafe` or `vercel` according to the answer used.
docs/configuration.md: If both requests fail, `error` names both HTTP or transport outcomes, each followed by up to 200 bytes of that provider's response body; without a fallback key, the TypeSafe error and its response excerpt are returned directly.
docs/configuration.md: On `clear`, firstmate takes the tool's answer as the matched rule, where `rule_<n>` is rules index `n-1`, a `fallback:` line names the rule actually resolved, and a below-floor note means `default`.
docs/configuration.md: Firstmate then resolves that rule's profile under its `select` mode above: `bin/fm-dispatch-select.sh` for an ordered rule, while the tool's own `profile:` line serves only an explicit `quota-balanced` rule and is passed unless firstmate states a reason to override, such as the brief's reasoning class or an eligible-unranked-candidate note.
docs/configuration.md: The tool's floor, approval, and quota evidence therefore bind an ordered rule only through the rule it names; every non-clear result returns to the full existing intake.
docs/configuration.md: The resolver copies both environment-provided keys into non-exported private variables and unsets `TYPESAFE_API_KEY` and `AI_GATEWAY_API_KEY` before launching child processes, and bootstrap does the same for `TYPESAFE_API_KEY`, so the secrets are absent from their child environments.
docs/configuration.md: It sends each key to `curl` only as a header read from a file descriptor, never on argv, and nothing prints, logs, or writes either key.
docs/configuration.md: The resolver fixes the direct endpoint at `https://api.typesafe.ai/v1/systemone`, the fallback endpoint at `https://ai-gateway.vercel.sh/typesafe/v1/systemone`, default confidence floor at 0.6, and each request timeout at 5 seconds.
docs/configuration.md: In that list, no-mistakes runs the validation pipeline, gh-axi and chrome-devtools-axi cover GitHub and browser operations, and tasks-axi plus quota-axi back backlog mutations and dispatch quota evidence: explicit `quota-balanced` selection plus the established-exhaustion facts firstmate supplies to ordered selection.
docs/configuration.md: An absent or too-old `quota-axi` reports `MISSING: quota-axi (install: npm install -g quota-axi)`; explicit `quota-balanced` selection cannot resolve without a compatible binary, and firstmate intake uses it to supply established-exhaustion facts for ordered selection, although `bin/fm-dispatch-resolve.sh` itself never invokes it.
docs/herdr-backend.md: The task's ordinary metadata must be absent, and the candidate must have exactly one tab and exactly one pane besides any plugin sidebar panes docked in that tab.
docs/herdr-backend.md: A sidebar pane here is exactly what task cleanup closes a sidebar-only tab for: labelled `Sidebar`, with no agent and an unknown or absent agent status.
docs/herdr-backend.md: Any other extra pane, including an agent pane labelled `Sidebar`, still preserves the candidate.
docs/herdr-backend.md: Firstmate immediately revalidates the same journal, metadata absence, workspace title and token uniqueness, one-tab and one-pane topology besides sidebar panes, exact pane relationship, absent agent, process proof, and non-target focus before calling the existing exact-pane focus-preserving close helper.
docs/herdr-backend.md: It closes only that pane and never calls workspace close; when only plugin sidebar panes remain, the helper's sidebar-only tab close described in the task cleanup rules above then removes the husk's tab and with it the husk workspace.
docs/herdr-backend.md: Pi, pi-signed, and Codex no longer launch the away daemon; their ordinary supervision session continues under the posture record.
docs/herdr-backend.md: Codex remains inside its bounded foreground checkpoint loop, which returns durable watcher output as a tool result without modifying the composer.
docs/herdr-backend.md: Another opted-in non-Pi home also skips the daemon for `/afk`; see [supervision-host.md](supervision-host.md).
docs/pi-supervision-branch.md: The ordinary cleanup of a task whose pull request has landed needs no relocation because it is the branch's own job in both postures: `bin/fm-branch-prompt.sh` names the `check: merge landed:` wake, and any later stale or inactive-outcome row on that task, as the moment to verify the post-merge machinery `AGENTS.md` section 7 requires and, once it passes, attempt `bin/fm-teardown.sh` without `--force` and report any refusal instead of concluding there is "nothing to recover".
docs/sessionstart-nudge.md: Interactive compaction and re-emit remain unverified; startup delivery does not establish those reset paths.
docs/sessionstart-nudge.md: The nudge payload starts with U+2063 and the stable `FIRSTMATE_OP: ` label and carries the current `session-start` protocol kind.
docs/sessionstart-nudge.md: Its ordinary body is exactly ``Run `bin/fm-session-start.sh` now, exactly once, before executing any other instructions.``, while the trusted Codex hook carries the same command with `--codex-hook` so the digest uses that hook's verified identity rather than process ancestry.
docs/supervision-host.md: - `/afk` launches no away daemon on an opted-in home of those harnesses, because the host is the away session there; `/quiet` still launches the daemon except on Codex, which runs no daemon and refuses `/quiet`, and while its flag `state/.afk` exists the host stands aside exactly as the plain arm does.
